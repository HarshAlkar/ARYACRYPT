# Frontend environment (copy to `.env.local`)

# Must use the SAME hostname as the browser (localhost vs 127.0.0.1).
# Mismatch breaks HttpOnly refresh cookies (treated as cross-site).
# If unset, the app defaults to http://<page-hostname>:8000/api/v1
VITE_API_BASE_URL=http://localhost:8000/api/v1
