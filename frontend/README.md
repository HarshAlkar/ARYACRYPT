# AryaCrypt Web Platform

React + Vite frontend for **ARYACRYPT**, a TIVRA cryptographic security framework.

```bash
npm install
npm run dev
```

Configure `VITE_API_BASE_URL` (see `.env.example`) to point at the FastAPI backend.

Developed by TIVRA · Created by Harsh Alkar
